const players=[],edges=[],playersMap=new Map,graph={players,edges},matchesList=document.querySelectorAll(".brkts-matchlist");matchesList.forEach(e=>{let r=e.querySelectorAll(".brkts-matchlist-match");r.forEach(e=>{let[r,t]=Array.from(e.querySelectorAll(".brkts-matchlist-cell .name")).map(e=>e.innerHTML).map(e=>{let r=playersMap.get(e.toLowerCase());if(r)return r;let t={name:e,id:players.length};return players.push(t),playersMap.set(e.toLowerCase(),t),t}),[l,s]=Array.from(e.querySelectorAll(".brkts-matchlist-score .brkts-matchlist-cell-content")).map(e=>Number.parseInt(e.innerHTML));for(let a=0;a<l;a++)edges.push({winner_id:r.id,loser_id:t.id});for(let i=0;i<s;i++)edges.push({winner_id:t.id,loser_id:r.id})})}),console.log(`Registered ${players.length} players and ${edges.length} matches`),console.log(JSON.stringify(graph));